<alfresco-config>
   
   <!-- Repository Library config section -->
   <config evaluator="string-compare" condition="RepositoryLibrary" replace="true">
      <!--
         Whether the link to the Repository Library appears in the header component or not.
      -->
      <visible>true</visible>
   </config>
   
	<!-- Custom Types & Aspects -->   
   	<config evaluator="string-compare" condition="DocumentLibrary" replace="false">
		
		<types>			
			<type name="cm:folder">
				<subtype name="cer:biblioteca" />			
			</type>
		</types>
		
		<metadata-templates>
			<!-- MBA PastaProcesso -->
			<template id="isBiblioteca">
				<evaluator>evaluator.doclib.action.isBiblioteca</evaluator>	
				<line index="10" id="numProcesso" view="detailed">{cer:numProcesso prop.cer_numProcesso}</line>
				<line index="20" id="situacao" view="detailed">{cer:situacao prop.cer_situacao}</line>
				<line index="30" id="partes" view="detailed">{cer:partes prop.cer_partes}</line>
				<line index="40" id="acao" view="detailed">{cer:acao prop.cer_acao}</line>
				<line index="40" id="vara" view="detailed">{cer:vara prop.cer_vara}</line>
				<line index="60" id="etiqueta" view="detailed">{cer:etiqueta prop.cer_etiqueta}</line>
				<line index="70" id="original" view="detailed">{cer:original prop.cer_original}</line>				
			</template>
		</metadata-templates>
   	</config>
	
	<config replace="true" evaluator="string-compare" condition="AdvancedSearch">
		<advanced-search>
			<!-- Forms for the advanced search type list -->
			<forms>
				<form label="Biblioteca de Processos" description="Pesquisa de Processos" sites="cerqueira-cesar,teste">cer:biblioteca</form>
				<form label="Documentos" description="Pesquisa de Documentos" sites="ANY">cm:document</form>
				<form label="Pastas" description="Pesquisa de Pastas">cm:folder</form>
			</forms>
		</advanced-search>
	</config>
	

	<!-- Formulário de busca para cer:biblioteca -->
	<config evaluator="model-type" condition="cer:biblioteca">
		<forms>
			<!-- Search form -->
			<form id="search">
				<field-visibility>
					<show id="cm:name" />
					<show id="cer:numProcesso" force="true" />
					<show id="cer:situacao" force="true" />
					<show id="cer:partes" force="true" />
					<show id="cer:acao" force="true" />
					<show id="cer:vara" force="true" />
					<show id="cer:etiqueta" force="true" />
					<show id="cer:original" force="true" />
				</field-visibility>
				<appearance>
					<field id="cer:numProcesso" label-id="prop.cer_numProcesso" />
					<field id="cer:situacao" label-id="prop.cer_situacao" />
					<field id="cer:partes" label-id="prop.cer_partes" />
					<field id="cer:acao" label-id="prop.cer_acao" />
					<field id="cer:vara" label-id="prop.cer_vara" />
					<field id="cer:etiqueta" label-id="prop.cer_etiqueta" />
					<field id="cer:original" label-id="prop.cer_original" />
				</appearance>
			</form>
		</forms>
	</config>
	
	<!-- cer:biblioteca type (existing nodes) -->
	<config evaluator="node-type" condition="cer:biblioteca">
		<forms>
			<!-- Formulário de propriedades da tela de detalhes do documento -->
			<form>
				<field-visibility>
					<show id="cm:name" />
					<show id="cer:numProcesso" force="true" />
					<show id="cer:situacao" force="true" />
					<show id="cer:partes" force="true" />
					<show id="cer:acao" force="true" />
					<show id="cer:vara" force="true" />
					<show id="cer:etiqueta" force="true" />
					<show id="cer:original" force="true" />
					<show id="cm:creator" for-mode="view" />
					<show id="cm:created" for-mode="view" />
					<show id="cm:modifier" for-mode="view" />
					<show id="cm:modified" for-mode="view" />
				</field-visibility>
				
				<appearance>
				<!-- Custom Fields -->
               		<field id="cer:numProcesso" label-id="prop.cer_numProcesso" />
					<field id="cer:situacao" label-id="prop.cer_situacao" />
					<field id="cer:partes" label-id="prop.cer_partes" />
					<field id="cer:acao" label-id="prop.cer_acao" />
					<field id="cer:vara" label-id="prop.cer_vara" />
					<field id="cer:etiqueta" label-id="prop.cer_etiqueta" />
					<field id="cer:original" label-id="prop.cer_original" />
				</appearance>
			</form>

			<!-- Formulário de edição de propriedades pop-up -->
			<form id="doclib-simple-metadata">
				<field-visibility>
					<show id="cm:name" />
					<show id="cer:numProcesso" force="true" />
					<show id="cer:situacao" force="true" />
					<show id="cer:partes" force="true" />
					<show id="cer:acao" force="true" />
					<show id="cer:vara" force="true" />
					<show id="cer:etiqueta" force="true" />
					<show id="cer:original" force="true" />						
				</field-visibility>			
				<appearance>
				<!-- Custom Fields -->
               		<field id="cer:numProcesso" label-id="prop.cer_numProcesso" />
					<field id="cer:situacao" label-id="prop.cer_situacao" />
					<field id="cer:partes" label-id="prop.cer_partes" />
					<field id="cer:acao" label-id="prop.cer_acao" />
					<field id="cer:vara" label-id="prop.cer_vara" />
					<field id="cer:etiqueta" label-id="prop.cer_etiqueta" />
					<field id="cer:original" label-id="prop.cer_original" /> 
				</appearance>
			</form>

			<!-- Formulário de edição de propriedades tela cheia -->
			<form id="doclib-inline-edit">
				<field-visibility>
					<show id="cer:numProcesso" force="true" />
					<show id="cer:situacao" force="true" />
					<show id="cer:partes" force="true" />
					<show id="cer:acao" force="true" />
					<show id="cer:vara" force="true" />
					<show id="cer:etiqueta" force="true" />
					<show id="cer:original" force="true" />					
				</field-visibility>	

				<appearance>
				<!-- Custom Fields -->
               		<field id="cer:numProcesso" label-id="prop.cer_numProcesso" />
					<field id="cer:situacao" label-id="prop.cer_situacao" />
					<field id="cer:partes" label-id="prop.cer_partes" />
					<field id="cer:acao" label-id="prop.cer_acao" />
					<field id="cer:vara" label-id="prop.cer_vara" />
					<field id="cer:etiqueta" label-id="prop.cer_etiqueta" />
					<field id="cer:original" label-id="prop.cer_original" />  
				</appearance>
			</form>
		</forms>
	</config>
  
   <config evaluator="string-compare" condition="Remote">
      <remote>
         <endpoint>
            <id>alfresco-noauth</id>
            <name>Alfresco - unauthenticated access</name>
            <description>Access to Alfresco Repository WebScripts that do not require authentication</description>
            <connector-id>alfresco</connector-id>
            <endpoint-url>http://localhost:8080/alfresco/s</endpoint-url>
            <identity>none</identity>
         </endpoint>

         <endpoint>
            <id>alfresco</id>
            <name>Alfresco - user access</name>
            <description>Access to Alfresco Repository WebScripts that require user authentication</description>
            <connector-id>alfresco</connector-id>
            <endpoint-url>http://localhost:8080/alfresco/s</endpoint-url>
            <identity>user</identity>
         </endpoint>

         <endpoint>
            <id>alfresco-feed</id>
            <name>Alfresco Feed</name>
            <description>Alfresco Feed - supports basic HTTP authentication via the EndPointProxyServlet</description>
            <connector-id>http</connector-id>
            <endpoint-url>http://localhost:8080/alfresco/s</endpoint-url>
            <basic-auth>true</basic-auth>
            <identity>user</identity>
         </endpoint>
      </remote>
   </config>

</alfresco-config>